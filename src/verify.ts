import type { SharedOptions } from "#shared/types";

// eslint-disable-next-line ts/no-empty-object-type
export interface VerifyOptions extends SharedOptions {}

export function verify(_options: VerifyOptions) {

}
